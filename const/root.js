const ROOT_HEADER = document.getElementById('header');
const ROOT_BANNER = document.getElementById('banner');

const ROOT_PRODUCTS = document.getElementById('products');
const ROOT_BESTSELLERS = document.getElementById('bestsellers');
const ROOT_NEW_COLLECTION = document.getElementById('new_collection');

const ROOT_BASKET = document.getElementById('basket');
